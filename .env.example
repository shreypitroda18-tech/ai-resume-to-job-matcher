# Optional: Google Gemini API Key
# If omitted, the application seamlessly runs using its intelligent built-in heuristic ATS engine.
# You can also set this directly inside the web application via the API Settings modal!
GEMINI_API_KEY=
